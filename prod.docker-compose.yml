version: '3.9'
services:
  nginx:
    build:
      context: .
      dockerfile: ./nginx.Dockerfile
      ports:
        - 80:80
      env_file:
        - .config/prod.env
  web:
    build: .
    stdin_open: true
    tty: true
    ports:
      - 5050:5050
    env_file:
      - .config/prod.env
